name: Build VoxelCraft

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - - name: Extrair projeto
  run: unzip -o voxelcraft-5.16.1-custom.zip

      - name: Listar arquivos
        run: ls -R
